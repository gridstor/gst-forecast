---
import Layout from '../../layouts/Layout.astro';
import { query } from '../../lib/db';

// Get recent curve activity
let recentActivity = [];
try {
  const result = await query(`
    SELECT 
      cd."curveName",
      ci."instanceVersion",
      ci.status,
      ci."createdAt",
      ci."createdBy",
      cd.market,
      cd.location
    FROM "Forecasts"."CurveInstance" ci
    JOIN "Forecasts"."CurveDefinition" cd ON ci."curveDefinitionId" = cd.id
    ORDER BY ci."createdAt" DESC
    LIMIT 10
  `);
  recentActivity = result.rows;
} catch (error) {
  console.error('Error fetching activity:', error);
}
---

<Layout title="Admin">
  <div class="min-h-screen bg-gray-50">
    <!-- Header with Dropdown Navigation -->
    <div class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-40">
      <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
          <h1 class="text-2xl font-bold text-gray-900">Admin</h1>
          
          <!-- Quick Nav Dropdown -->
          <div class="relative group">
            <button class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 flex items-center gap-2">
              Quick Nav
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
              </svg>
            </button>
            <div class="absolute right-0 mt-2 w-56 bg-white rounded-lg shadow-xl border border-gray-200 hidden group-hover:block">
              <a href="#curve-schedule" class="block px-4 py-2 text-sm text-gray-700 hover:bg-blue-50">📅 Curve Schedule</a>
              <a href="#curve-management" class="block px-4 py-2 text-sm text-gray-700 hover:bg-blue-50">📊 Curve Management</a>
              <a href="#documentation" class="block px-4 py-2 text-sm text-gray-700 hover:bg-blue-50">📚 Documentation</a>
              <a href="#dev-ops" class="block px-4 py-2 text-sm text-gray-700 hover:bg-blue-50">⚙️ Development Ops</a>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="container mx-auto px-4 py-8 max-w-[1600px]">
      
      <!-- SECTION: CURVE SCHEDULE -->
      <section id="curve-schedule" class="mb-12">
        <h2 class="text-2xl font-bold text-gray-900 mb-6">📅 Curve Schedule</h2>
        
        <!-- Latest Activity Log -->
        <div class="bg-white rounded-lg shadow-md p-6 mb-6">
          <h3 class="text-lg font-semibold text-gray-900 mb-4">Latest Curve Activity</h3>
          <div class="space-y-2">
            {recentActivity.slice(0, 5).map((activity) => (
              <div class="flex items-center justify-between py-2 border-b border-gray-100 last:border-0">
                <div class="flex-1">
                  <div class="font-medium text-gray-900 text-sm">{activity.curveName}</div>
                  <div class="text-xs text-gray-600">{activity.instanceVersion} • {activity.market} • {activity.location}</div>
                </div>
                <div class="text-right">
                  <div class="text-xs text-gray-500">{new Date(activity.createdAt).toLocaleDateString()}</div>
                  <div class="text-xs">
                    <span class={`px-2 py-0.5 rounded-full ${activity.status === 'ACTIVE' ? 'bg-green-100 text-green-800' : 'bg-gray-100 text-gray-800'}`}>
                      {activity.status}
                    </span>
                  </div>
                </div>
              </div>
            ))}
          </div>
          <a href="/admin/curve-logs" class="mt-4 inline-block text-sm text-blue-600 hover:text-blue-800">View All Activity →</a>
        </div>

        <!-- Calendar View -->
        <div class="bg-white rounded-lg shadow-md p-6">
          <div class="flex items-center justify-between mb-4">
            <h3 class="text-lg font-semibold text-gray-900">Curve Schedule Calendar</h3>
            <a href="/admin/curve-schedule" class="px-4 py-2 bg-purple-600 text-white rounded-md hover:bg-purple-700 text-sm">
              Open Full Calendar
            </a>
          </div>
          <p class="text-gray-600 text-sm">View and manage scheduled curve updates and delivery periods</p>
        </div>
      </section>

      <!-- SECTION: CURVE MANAGEMENT -->
      <section id="curve-management" class="mb-12">
        <h2 class="text-2xl font-bold text-gray-900 mb-6">📊 Curve Management</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <a href="/admin/upload" class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow border border-blue-200">
            <div class="flex items-center mb-3">
              <div class="p-3 bg-blue-600 rounded-lg">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"/>
                </svg>
              </div>
              <h3 class="ml-4 text-lg font-semibold text-gray-900">Upload New Curves</h3>
            </div>
            <p class="text-gray-600 text-sm">Create definitions, instances, and upload price data</p>
          </a>

          <a href="/admin/inventory" class="bg-gradient-to-br from-orange-50 to-orange-100 rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow border border-orange-200">
            <div class="flex items-center mb-3">
              <div class="p-3 bg-orange-600 rounded-lg">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4"/>
                </svg>
              </div>
              <h3 class="ml-4 text-lg font-semibold text-gray-900">Manage Inventory</h3>
            </div>
            <p class="text-gray-600 text-sm">View, edit, and delete curves, instances, and data</p>
          </a>
        </div>
      </section>

      <!-- SECTION: DOCUMENTATION -->
      <section id="documentation" class="mb-12">
        <h2 class="text-2xl font-bold text-gray-900 mb-6">📚 Documentation</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          
          <a href="/admin/docs" class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow border border-gray-200">
            <div class="flex items-center mb-3">
              <div class="p-2 bg-indigo-100 rounded-lg">
                <svg class="w-5 h-5 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4"/>
                </svg>
              </div>
              <h3 class="ml-3 font-semibold text-gray-900">Curves Schema</h3>
            </div>
            <p class="text-gray-600 text-sm">Database schema for CurveDefinition, CurveInstance, and CurveData tables</p>
          </a>

          <div class="bg-white rounded-lg shadow-md p-6 border border-gray-200">
            <div class="flex items-center mb-3">
              <div class="p-2 bg-purple-100 rounded-lg">
                <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                </svg>
              </div>
              <h3 class="ml-3 font-semibold text-gray-900">Model Documentation</h3>
            </div>
            <p class="text-gray-600 text-sm">Forecast models, methodologies, and calculation details</p>
          </div>

          <div class="bg-white rounded-lg shadow-md p-6 border border-gray-200">
            <div class="flex items-center mb-3">
              <div class="p-2 bg-teal-100 rounded-lg">
                <svg class="w-5 h-5 text-teal-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/>
                </svg>
              </div>
              <h3 class="ml-3 font-semibold text-gray-900">Code & Data Flow</h3>
            </div>
            <p class="text-gray-600 text-sm">Code documentation, data sources, and system architecture</p>
          </div>
        </div>
      </section>

      <!-- SECTION: DEVELOPMENT OPS -->
      <section id="dev-ops" class="mb-12">
        <h2 class="text-2xl font-bold text-gray-900 mb-6">⚙️ Development Ops</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          
          <a href="/admin/db-health" class="bg-gradient-to-br from-green-50 to-green-100 rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow border border-green-200">
            <div class="flex items-center mb-3">
              <div class="p-3 bg-green-600 rounded-lg">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
              </div>
              <h3 class="ml-4 text-lg font-semibold text-gray-900">Database Health</h3>
            </div>
            <p class="text-gray-600 text-sm">Run comprehensive health checks on database connections and tables</p>
          </a>

          <a href="/admin/api-health" class="bg-gradient-to-br from-red-50 to-red-100 rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow border border-red-200">
            <div class="flex items-center mb-3">
              <div class="p-3 bg-red-600 rounded-lg">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.111 16.404a5.5 5.5 0 017.778 0M12 20h.01m-7.08-7.071c3.904-3.905 10.236-3.905 14.141 0M1.394 9.393c5.857-5.857 15.355-5.857 21.213 0"/>
                </svg>
              </div>
              <h3 class="ml-4 text-lg font-semibold text-gray-900">API Health Check</h3>
            </div>
            <p class="text-gray-600 text-sm">Monitor API endpoints, response times, and system status</p>
          </a>
        </div>
      </section>

    </div>
  </div>

  <!-- Toast Notification -->
  <div id="toast" class="hidden fixed top-4 right-4 z-50 px-6 py-3 rounded-lg shadow-lg text-white"></div>
</Layout>

<script>
  // Smooth scroll to sections
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href')!);
      if (target) {
        target.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }
    });
  });
</script>
</Layout>
